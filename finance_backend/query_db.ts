import mysql from 'mysql2/promise'; import dotenv from 'dotenv'; dotenv.config(); async function run() { const db = mysql.createPool({ host: process.env.DB_HOST, user: process.env.DB_USER, password: process.env.DB_PASSWORD, database: process.env.DB_NAME, ssl: { rejectUnauthorized: false } }); try { const [rows] = await db.query(\SELECT VendorName, AccountHolderName, AccountNumber, IFSCCode, BankName, BranchName FROM vendor WHERE VendorName LIKE '%Bluwheelz%'\); console.log(rows); } catch (e) { console.error(e); } process.exit(0); } run();
